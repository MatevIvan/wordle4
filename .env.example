# Used only by the local Vite development proxy.
WORD_CHECKER_API_KEY=your_rapidapi_key

# Production URL of a server-side proxy that injects the RapidAPI key.
VITE_WORD_CHECKER_PROXY_URL=https://your-proxy.example.com/api/word-checker
